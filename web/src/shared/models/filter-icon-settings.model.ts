export class FilterIconSettings {
	name: string;
	nextIcon: string;
	url: string;

	constructor(name: string, nextIcon: string, url: string) {
		this.name = name;
		this.nextIcon = nextIcon;
		this.url = url;
	}
}
